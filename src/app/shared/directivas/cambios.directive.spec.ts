import { CambiosDirective } from './cambios.directive';

describe('CambiosDirective', () => {
  it('should create an instance', () => {
    //const directive = new CambiosDirective();
   // expect(directive).toBeTruthy();
  });
});
